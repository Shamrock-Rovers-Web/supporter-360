{
  "name": "supporter360",
  "version": "1.0.0",
  "description": "Supporter 360 - Single pane of glass for Shamrock Rovers supporter data",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@supporter360/shared && npm run build --workspace=@supporter360/database && npm run build --workspace=@supporter360/backend",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "dev:frontend": "npm run dev --workspace=@supporter-360/frontend",
    "validate": "./scripts/validate-build.sh"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "@types/node": "^20.10.0",
    "playwright": "^1.58.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
